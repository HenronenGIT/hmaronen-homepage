export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">New homepage workspace</p>
        <h1>Redesign in progress.</h1>
        <p className="summary">
          This app is now isolated from the legacy homepage so the new visual
          direction can evolve without touching the time-machine version.
        </p>
      </section>
    </main>
  );
}
