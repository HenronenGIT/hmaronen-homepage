echo "Running pre-push checks..."

# Run type checking
echo "Running workspace checks..."
pnpm check-all || {
  echo "Pre-push checks failed. Please fix the reported issues before pushing."
  exit 1
}

echo "All pre-push checks passed."
